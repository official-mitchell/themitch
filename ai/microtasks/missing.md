Radix components — shadcn/ui adds these per-component when you run npx shadcn@latest add button etc.
Chart libraries — add recharts later when you need it
Carousel/masonry — add when specific features require them
